

local function grid()
    local GridContainer = Instance.new("Frame")
        GridContainer.Size = GridContentContainer + GridGap / fr

        local GridContentContainer = Instance.new("Frame")









        if Element == display: grid() then


end