
local url = 
local geometrybox = {margin-box,
                     border-box,
                     padding-box,
                     content-box,
                     fill-box,
                     stroke-box,
                     view-box
}
local basicshape = {local function inset(),
                    local function inset(),
                    local function inset(),
                    local function inset()
}

local function ClipPath(url, geometrybox, basicshape, BoxShapeCombValues, GobalValues)