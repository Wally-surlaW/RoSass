
local Selectors = require(script.Parent.Selectors)

local function SelectorType(.,:,::,&)
    local function GlobalizeSelector(SelectorType)
        if SelectorType == .,:,::,& then
            
    end
end